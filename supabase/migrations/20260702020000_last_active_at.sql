-- ============================================================
-- profiles.last_active_at: ユーザーの最終アクティブ時刻
--   開発タブ「オンライン」の「最近のアクセス」表示に使う。
--   クライアントが 5 分ごと（＋接続時）に自分の行を now() で更新する。
--
-- RLS は既存のまま:
--   - 本人更新ポリシーで自分の last_active_at を更新できる
--   - 管理者/開発者は全件参照ポリシーで全ユーザーの last_active_at を閲覧できる
-- ============================================================
alter table public.profiles add column if not exists last_active_at timestamptz;
