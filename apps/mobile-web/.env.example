# Supabase（ローカル開発で接続する先）
# .env.example をコピーして .env を作り、値を埋める。
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# 本番に繋ぎたい時に上の値と入れ替えて使う用（任意）
PUBLIC_SUPABASE_URL_PROD=
PUBLIC_SUPABASE_ANON_KEY_PROD=

# 招待制: true で新規登録UIを無効化（招待されたユーザーのみログイン可）
PUBLIC_DISABLE_SIGNUP=true
